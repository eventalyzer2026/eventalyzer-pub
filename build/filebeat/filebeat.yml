filebeat.inputs:
  - type: filestream
    id: apache
    paths: ["/var/log/test_logs/*access.log"]
    encoding: utf-8
    fields_under_root: true

output.logstash:
  hosts: ["logstash:5044"]
  ssl.enabled: false
